using System;
class RPS {
  static void Main() {
    //
    int playerScore = 0;
    string playerChoice = "";
    int numDraws = 0;

    //cpu variables 
    int cpuScore = 0;
    string cpuChoice = "";
    int loopCount = 0;
    int loopsReqs = 0; //req / reqs is universal abbreviations for request
    Console.WriteLine("how many loos do you need? type and integer and press enter");
    loopsReqs = Convert.ToInt32(Console.ReadLine);
    while ()
  }
}